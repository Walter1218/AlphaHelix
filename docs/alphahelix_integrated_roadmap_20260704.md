# AlphaHelix 整合路线图：量化模型 + LLM 双引擎

> 文档日期：2026-07-05  
> 状态：Phase 1 ✅ 已完成；Phase 2 ✅ 已完成但未达提升目标；Phase 3 ✅ 已完成但未达提升目标；Phase 4 ✅ 已初步迭代（因子生成、宏观择时、事件过滤、walk-forward 阈值校准、头寸加权已实验），真实文本数据源与 memory_search 仍阻塞  
> 目标：把传统量化 ML 与 LLM 能力整合成一套可验证、可进化的 A 股选股系统

---

## 1. 执行摘要

AlphaHelix 已经跑通了数据层、因子层、评估层和 Agent 层，但核心预测能力尚未建立：

- 当前基于线性加权打分的 Pass2 无法产生稳定 alpha；
- 在线 Logistic 回归二分类在 5 日方向预测上失效；
- LLM 目前只用于最终报告生成，未充分发挥其在文本/事件/因子挖掘上的能力。

本方案提出**"量化模型为主、LLM 为辅"**的架构：

- **主引擎**：Gradient Boosting（LightGBM/XGBoost）做 cross-sectional 收益率/排序预测；
- **辅助引擎**：LLM 从新闻、公告、研报中提取情感/事件/叙事信号，并辅助因子代码生成；
- **决策层**：量化模型输出候选池与得分，LLM 做定性风控与叙事校验；
- **进化层**：用 IC/IR、分位数收益、回测超额驱动迭代。

---

## 2. 当前系统诊断

| 模块 | 现状 | 问题 | 改进方向 |
|---|---|---|---|
| 数据层 | Tushare 量价、估值、财务、资金、事件数据已接入 | 缺少北向、融资融券、龙虎榜、披露日预告、新闻 sentiment | 补齐另类数据 |
| 因子层 | 18+ 因子，rank 标准化后线性加权 | 无行业/市值中性化，原始资金金额量纲问题 | GBDT + 中性化 + LLM 文本因子 |
| 策略层 | 4 个硬编码策略 + regime 切换 | regime 映射僵化，几乎总是 event_driven | 用模型输出替代硬编码映射 |
| 预测层 | 在线 Logistic 回归二分类 | 5 日方向噪声大，模型未学到信号 | 改用 GBDT 回归/排序预测 |
| 持仓层 | Top-N 强制满仓 | 没有空仓机制，方向准确率失真 | 阈值制 + 空仓 + 滚动再平衡 |
| LLM 层 | 生成选股报告 | 未用于信号提取、因子生成、RAG | 接入情感/事件/RAG/多 Agent 风控 |
| 评估层 | 方向准确率、超额收益、最大回撤 | 缺少 IC/IR、分位数收益、交易成本 | 补全量化评估体系 |

---

## 3. 整合架构

```
┌─────────────────────────────────────────────────────────────┐
│                        数据层                                │
│  Tushare 量价/估值/财务/资金/事件  +  新闻/公告/研报/政策     │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      特征工程层                              │
│  数值因子（动量/估值/质量/资金/行业）                         │
│  截面 rank / 行业市值中性化 / 时序平稳化                      │
│  LLM 因子（情感/事件/叙事/管理层语调）                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      预测模型层                              │
│  主模型：LightGBM/XGBoost 预测未来收益率/排序                 │
│  辅助：LLM 生成候选因子代码 → 回测筛选                        │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      组合构建层                              │
│  按预测得分分位数分组 → top-k 持仓                            │
│  行业/市值约束 + 换手率惩罚 + 交易成本                        │
│  LLM 风控：叙事风险/业绩暴雷/政策风险                         │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      评估与进化层                            │
│  IC / IR / 分位数收益 / 回测超额 / 最大回撤                   │
│  Walk-forward / Purged CV / Deflated Sharpe                 │
└─────────────────────────────────────────────────────────────┘
```

---

## 4. 数据层升级

### 4.1 已接入

- `daily`、`daily_basic`、`fina_indicator`、`moneyflow`、`forecast`、`express`
- `index_daily`、`trade_cal`、`stock_basic`、`namechange`

### 4.2 待接入（按优先级）

| 数据 | Tushare API | 用途 |
|---|---|---|
| 融资融券 | `margin` | 杠杆情绪、市场热度 |
| 北向资金 | `moneyflow_hsgt` | 外资流向 |
| 龙虎榜 | `top_list` / `top_inst` | 游资/机构异动 |
| 披露日预告 | `disclosure_date` | 事件前置布局、避雷 |
| 涨跌停 | `limit_list` | 市场情绪、流动性风险 |
| 新闻/公告 | 外部爬虫或 API | LLM 情感/事件因子 |
| 卖方研报 | 外部 API | LLM 评级/目标价/情感 |

---

## 5. 特征工程升级

### 5.1 数值因子处理

1. **截面 rank 标准化**：每只因子在当日截面上转换为 0~1 分位数。
2. **行业/市值中性化**：
   - 对每个因子，用当日截面回归去除行业和市值解释部分；
   - 保留残差作为纯净 alpha 信号。
3. **时序平稳化**：
   - 对动量类因子做分数差分或相对变化，降低非平稳性。
4. **移除问题因子**：
   - 去掉原始资金净流入金额（`net_mf_5d`、`net_mf_20d`）；
   - 保留 `net_mf_ratio`、`net_mf_divergence`。

### 5.2 LLM 因子

| 因子 | 输入 | LLM 输出 | 用途 |
|---|---|---|---|
| 情感得分 | 新闻、公告、研报 | -1 ~ 1 的情感分数 | 情绪面 alpha |
| 事件类型 | 业绩预告、政策、并购 | 标签（预增/暴雷/政策利好等） | 事件驱动 |
| 管理层语调 | 业绩说明会纪要 | 乐观/中性/谨慎 | 质量/预期因子 |
| 叙事风险 | 新闻、社交媒体 | 高波动/高杠杆/监管风险标签 | 风控过滤 |
| 行业景气 | 行业新闻、政策 | 景气度评分 | 行业配置 |

### 5.3 因子生成 Agent

参考 Microsoft RD-Agent：

1. LLM 阅读研报/论文/财报，提取潜在因子公式；
2. 自动生成 Python 代码并接入 `screen.py`；
3. 在 walk-forward 框架下计算 IC；
4. IC 显著的因子纳入主模型；不显著的淘汰。

---

## 6. 预测模型层

### 6.1 主模型：LightGBM / XGBoost

- **目标变量**：未来 5/10/20 个交易日相对沪深300 的超额收益（回归）。
- **备选目标**：未来收益率分位数（分类/排序）。
- **训练方式**：
  - 滚动训练：每月用过去 12~24 个月数据训练；
  - 或用 Qlib 式的 walk-forward 重训练。
- **特征输入**：数值因子 + LLM 因子。
- **样本权重**：
  - 按时间衰减，近期样本权重更高；
  - 或按流动性/市值加权。

### 6.2 为什么不用在线 Logistic 回归

- 5 日方向二分类噪声过大；
- 线性模型无法捕捉因子交互；
- GBDT 对异常值、缺失值、非线性更鲁棒；
- 行业验证 GBDT 是量化选股强基线。

### 6.3 在线学习（第二阶段）

在 GBDT 基线稳定后，再考虑：

- 增量更新 LightGBM（如 `lightgbm` 的 `refit`）；
- 或保留在线线性模型作为 GBDT 的残差修正。

---

## 7. 组合构建层

### 7.1 从预测到持仓

1. 每天对全市场候选股打分；
2. 按预测得分分 5/10 组，先验证 group 单调性；
3. 取 top-k 作为初始持仓池；
4. 应用约束：
   - 单一行业 ≤ 40%（市值权重）；
   - 单票 ≤ 20%；
   - 换手率惩罚，避免频繁调仓；
   - 预测得分低于阈值时空仓。

### 7.2 LLM 风控介入点

- **选股后**：对 top-k 候选做叙事风险、业绩暴雷、政策风险检查；
- **可执行动作**：
  - 否决某只股票；
  - 降低权重；
  - 增加风险提示。
- **不可执行动作**：
  - 凭空推荐未在候选池的股票。

### 7.3 交易成本

回测中必须扣除：

- 印花税 0.1% 单边（卖出）；
- 佣金 0.02% 双边；
- 滑点 0.05%~0.1% 双边。

---

## 8. 评估体系

### 8.1 预测质量

| 指标 | 说明 | 目标 |
|---|---|---|
| rank IC | 预测得分与未来收益的秩相关 | > 0.03 |
| ICIR | IC 均值 / IC 标准差 | > 0.3 |
| IC 衰减 | 多期 IC 持续性 | 衰减不要太快 |
| 分位数收益 | top 组 vs bottom 组收益差 | 单调且显著 |

### 8.2 组合绩效

| 指标 | 说明 | 目标 |
|---|---|---|
| 年化超额收益 | 相对沪深300 | > 5% |
| 信息比率 IR | 超额收益 / 跟踪误差 | > 0.8 |
| 最大回撤 | 组合净值高点到低点 | < 15% |
| 胜率 | 超额收益 > 0 的月份占比 | > 50% |
| 换手率 | 年均换手 | < 10 倍 |

### 8.3 过拟合控制

- Purged walk-forward CV；
- Deflated Sharpe Ratio；
- 训练集/验证集/测试集严格分离；
- 因子/模型调参只在训练集上做。

---

## 9. LLM Agent 角色重定义

### 9.1 alpha-analyst 职责

| 阶段 | LLM 任务 | 输出 |
|---|---|---|
| 数据层 | 不直接计算 | 调用工具获取数据 |
| 特征层 | 生成文本/事件因子 | 情感分数、事件标签 |
| 模型层 | 提出因子假设、生成代码 | 新因子代码 |
| 组合层 | 叙事风控、风险提醒 | 否决/降权建议 |
| 报告层 | 生成投资逻辑与报告 | Markdown/JSON |

### 9.2 多 Agent 风控（未来）

- **宏观分析师**：判断 regime、流动性、政策方向；
- **行业分析师**：评估行业景气度与竞争格局；
- **个股分析师**：解读财报、公告、新闻；
- **风控官**：检查集中度、止损、叙事风险；
- **量化引擎**：提供候选池与得分。

---

## 10. 实施路线图

### ✅ Phase 1：基线重建（2~3 周）

1. 改造 `screen.py`：
   - 输出截面 rank 因子；
   - 加入行业/市值中性化；
   - 移除原始资金金额因子。
2. 新增 `scripts/model_trainer.py`：
   - 用 LightGBM 训练未来 5/10/20 日超额收益回归模型；
   - 滚动训练 + walk-forward 预测。
3. 新增 `scripts/predictor_eval.py`：
   - 计算 rank IC、ICIR、分位数收益。
4. 改造 `walkforward.py`：
   - 用模型预测得分替代在线 Logistic 回归；
   - 支持阈值制空仓、行业约束、交易成本。

**Phase 1 实测结果（2026-07-05）**：

| 配置 | 结果 |
|---|---|
| 模型 | LightGBM 滚动训练（12 个月窗口，按月重训练） |
| 目标 | 未来 10 日相对沪深300 超额收益 |
| 特征 | 23 个 rank/行业市值中性化因子 |
| 样本 | 2024-01 ~ 2026-06，预测 106 个日期，8,222 条 |
| Rank IC（日均） | 0.0469 |
| ICIR | 0.238 |
| 正 IC 比例 | 59.4% |
| 分位数收益（0→4） | -0.20%, +0.21%, +0.34%, +0.64%, +0.38%（单调） |
| 组合（20 只等权，10 日再平衡，含交易成本） | 平均超额 +0.21%，累计超额 +28.13%，胜率 54.3%，换手率 63.7%/期 |

> 结论：Phase 1 验收通过。滚动 GBDT 显著优于静态因子加权（基准累计超额 -16.65%）。下一步进入 Phase 2 数据补齐。

### ✅ Phase 2：数据补齐（2 周）

1. 接入 `margin`、`moneyflow_hsgt`、`top_list`、`disclosure_date`；
2. 构建资金流/情绪类因子；
3. 验证新因子对 IC 的提升。

**验收标准**：新因子组合 rank IC > 0.03。

**Phase 2 实测结果（2026-07-05）**：

| 配置 | rank IC（日均） | 说明 |
|---|---|---|
| Phase 1 基线（23 个特征） | **0.0469** | LightGBM 滚动训练 |
| + margin / northbound（宏观） | -0.0012 | 宏观变量截面中性化后产生伪波动，反而稀释 alpha |
| + 仅 disclosure + top_list 个股特征 | 0.0235 | 略低于基线 |
| + disclosure 事件 dummy + 龙虎榜 5 日聚合 | **0.0345** | 最接近基线，但未超越 |
| XGBoost on v2 | 0.0207 | 未超越 |

关键发现：
- `days_to_disclosure` 单因子 IC 为 -0.062，`disclosure_near`（<=5 天）单因子 IC 为 +0.060，事件效应显著但极不稳定；
- 龙虎榜 5 日聚合因子整体为负 IC（-0.03 左右），可能意味着游资/机构异动后短期反转；
- 融资融券/北向资金为市场层面变量，直接加入截面模型会引入与市值/行业相关的伪信号，应作为宏观/交互变量另行处理。

> 结论：Phase 2 **未达预期提升**。基础量价/财务/预期因子已较完整，新增另类数据在这个窗口内未能稳定增强 alpha。建议：
> 1. 把宏观变量从截面特征中剥离，改为 regime/交互信号；
> 2. 把 `disclosure_near` 作为事件过滤/增强条件，而非普通特征；
> 3. 进入 Phase 3 引入 LLM 文本/事件因子，作为更独立的信号源。

### ✅ Phase 2.5：风控增强（1 周）

已尝试在 `portfolio_backtest.py` 中加入个股止损逻辑，并接入 `walkforward_gbdt.py`。

| 止损线 | 平均超额 | 累计超额 | 胜率 | 平均每期止损次数 |
|---|---|---|---|---|
| 无止损 | +0.28% | +39.50% | 52.4% | 0.00 |
| -3% | +0.10% | +11.80% | 49.5% | 6.69 |
| -5% | +0.17% | +22.16% | 51.4% | 3.90 |
| -7% | +0.22% | +30.30% | 49.5% | 2.32 |
| -10% | +0.24% | +32.00% | 52.4% | 1.11 |
| -15% | +0.26% | +36.82% | 52.4% | 0.27 |

**结论**：固定百分比止损在这个策略中**没有提升胜率**，反而因频繁触发止损、提前锁定亏损、现金闲置和交易成本而侵蚀收益。策略的 alpha 主要来自 **正偏分布**（少数大涨覆盖多数小跌），硬止损会破坏这一结构。

后续可尝试：
- 组合层面止损（如净值回撤超阈值减仓），而非个股硬止损；
- 动态止损（ATR、波动率自适应）；
- 事件/宏观过滤，避免在高波动 regime 满仓。

### ✅ Phase 3：LLM / 自动因子挖掘（2~3 周）

**Phase 3.1：LambdaRank + 复合因子（2026-07-05 已完成初步迭代）**

- 在 `model_trainer.py` 中加入 `lambdarank` 目标，直接优化“头部排在底部前面”。
- 加入 `scripts/factor_miner.py`，支持用 YAML 表达式批量评估因子 IC；
- 从候选表达式中筛出一批高 IC 复合因子：

| 因子 | 表达式 | 平均 IC | ICIR |
|---|---|---|---|
| defensive_quality | `roe / (volatility_20 + 1e-9)` | **+0.093** | +3.80 |
| smart_money_per_risk | `net_mf_ratio / (volatility_20 + 1e-9)` | +0.067 | +3.06 |
| quality_growth | `roe * profit_growth` | +0.067 | +3.67 |
| value_quality | `dv_ratio * roe` | +0.064 | +2.66 |

- 把复合因子集成进 `screen.py` / `build_dataset.py`，重新训练生产模型 `gbdt_latest_h10.lightgbm.txt`。

**回测结果对比（walk-forward, 20 只等权，10 日再平衡）**：

| 模型 | 平均超额 | 累计超额 | 胜率 | Top20 个股命中率 |
|---|---|---|---|---|
| 基线 GBDT（23 特征） | +0.21% | +28.1% | 54.3% | 48.2% |
| LambdaRank（23 特征） | +0.14% | +15.8% | 48.6% | 48.3% |
| **GBDT + 复合因子（30 特征）** | **+0.29%** | **+40.2%** | **53.3%** | **48.5%** |

> 结论：LambdaRank 单独没有提升 portfolio 收益，但复合因子显著增强了选股信号，累计超额从 28% 提升到 40%。胜率仍停留在 53% 左右，说明 10 日持有期本身方向胜率天花板较低，需要通过更长持有期或更强独立信号来突破。

**下一步（Phase 3.2）**：
1. 接入真实文本/事件数据源（新闻、公告、研报），用 LLM 提取情感和事件标签；
2. 把 `factor_miner.py` 与 LLM 连接：LLM 根据研报/论文生成表达式 → 本地 IC 评估 → 达标后自动加入因子库；
3. 尝试 20 日持有期模型，看胜率是否提高。

**20 日持有期测试结果（2026-07-05）**：

| 模型 | 平均 IC | 平均超额 | 累计超额 | 胜率 | Top20 命中率 |
|---|---|---|---|---|---|
| GBDT 回归 h20 | 0.0223 | -0.18% | -26.4% | 40.4% | 43.1% |
| LambdaRank h20 | 0.0411 | — | — | — | — |
| **GBDT 回归 h10（当前最优）** | **0.0527** | **+0.29%** | **+40.2%** | **53.3%** | **48.5%** |

> 结论：**20 日持有期并未提升胜率**，反而因信号衰减、波动放大导致 IC 和 portfolio 收益都下降。当前最优仍是 **h10 + 复合因子 + 回归**。胜率瓶颈不在持有期长度，而在独立信号源不足。

### Phase 3.2：更多另类数据 engineered 特征（2026-07-05）

新增脚本 `scripts/append_phase3_features.py`，在 Phase 2 v2 基础上加入：
- 融资融券余额及 5/20 日变化率（`margin_total_balance`, `margin_change_5d`, `margin_change_20d`）
- 北向资金当日/5日/20日净流入及 zscore（`northbound_net_today`, `northbound_net_5d_sum`, `northbound_net_20d_sum`, `northbound_net_20d_zscore`, `northbound_net_5d_vs_20d`）
- 龙虎榜 5日/10日窗口聚合（次数、累计净流入、平均 amount_rate / turnover_rate / pct_change）

宏观特征保留为日期维度原始值，不经过截面 rank/中性化；个股龙虎榜特征按 0 填充缺失。

**回测结果（walk-forward, h=10, 20 只等权）**：

| 数据集 | 特征数 | 平均 IC | 平均超额 | 累计超额 | 胜率 | Top20 命中率 |
|---|---|---|---|---|---|---|
| composite（仅复合因子） | 30 | **0.0623** | **+0.29%** | **+40.2%** | **53.3%** | **48.5%** |
| + Phase 2 v2（披露日 + 龙虎榜20日） | 40 | 0.0327 | — | — | — | — |
| + Phase 3（宏观 + 龙虎榜5/10日） | 58 | 0.0255 | -0.25% | -33.1% | 40.0% | 45.8% |

> 结论：**更多另类数据没有提升模型**。可能原因：
> - `disclosure` / `top_list` 信号本身不稳定，加入后反而稀释有效复合因子；
> - 宏观变量作为日期常数特征，使树模型过度按日期切片，降低泛化；
> - 龙虎榜 5日/10日 与已有 `smart_money_per_risk` 高度相关，信息冗余。
>
> 当前最优模型仍是 **composite-only（30 特征）**。另类数据更适合作为**事件过滤/宏观择时**使用，而非直接堆进截面模型。

### Phase 3.3：LLM 因子生成 Agent（2026-07-05）

新增 `scripts/factor_agent.py`：
- 支持 `--prompt` / `--url` / `--seed-file` 输入；
- 未配置 `OPENAI_API_KEY` 时使用 fallback 经验表达式生成器；
- 调用 `factor_miner.evaluate_factor` 计算 IC；
- 对 top 因子做乘/除/加组合，生成第二代并评估。

运行示例：
```bash
python scripts/factor_agent.py \
  --prompt "Generate cross-sectional A-share factors combining momentum, volatility, quality, and smart money flow" \
  --dataset memory/dataset/features_h10_composite.parquet \
  --horizon 10 --second-gen --ic-threshold 0.02
```

**生成因子 IC 结果（样本内）**：

| 因子 | 表达式 | 平均 IC | ICIR |
|---|---|---|---|
| smart_money_per_risk * quality_growth | 复合因子乘积 | +0.0946 | +4.69 |
| defensive_quality * smart_money_per_risk | 复合因子乘积 | +0.0915 | +3.73 |
| quality_growth / volatility | 复合因子加波动率除法 | +0.0937 | +4.53 |
| (earnings_surprise_momentum + smart_money_per_risk)/2 + quality_growth/volatility | 第二代组合 | +0.1000 | +4.09 |

**但加入 GBDT 后的 walk-forward 表现**：

| 模型 | 平均 IC | 平均超额 | 累计超额 | 胜率 | Top20 命中率 |
|---|---|---|---|---|---|
| composite only | **0.0623** | **+0.29%** | **+40.2%** | **53.3%** | **48.5%** |
| composite + top 5 LLM 组合因子 | 0.0381 | +0.06% | +4.3% | 47.6% | 46.5% |

> 结论：**高 IC 的表达式组合进入 GBDT 后没有提升 portfolio，反而明显变差**。原因很可能是：
> - 这些组合因子是已有 composite 因子的非线性变换，与现有特征高度共线；
> - GBDT 本身已能捕捉同类交互，额外加入导致过拟合；
> - 样本内 IC 高不代表 walk-forward 有增量。
>
> 下一步要让 LLM 因子真正有效，需要：
> 1. **限制 LLM 只使用原始特征**（不含 composite），生成本质上新的信号；
> 2. 加入特征选择：只在 walk-forward 验证集上 IC 稳定的因子才进模型；
> 3. 引入正则化更强的模型（如 XGBoost + L1）或 PCA 去相关。

### Phase 3.4：LLM 文本/事件风控过滤器（scaffold）

新增 `scripts/llm_event_filter.py`：
- 输入日期 + 候选股票列表；
- 尝试拉取 `major_news` / `news` 等 Tushare 个股新闻接口；
- 用 LLM 对近期文本打分（-1 利空 ~ +1 利好），输出 `event_risk_score`；
- 可设置 `risk_threshold` 剔除高风险标的。

**当前状态**：脚本可运行，但由于缺少稳定可访问的个股新闻/公告数据源（免费 Tushare 无此接口），实际 `has_text=False`，LLM 无法输出有效分数。

**要启用该过滤器，需要接入**：
- Tushare 付费资讯接口（`major_news`, `news` 等）；或
- 外部公告/新闻 API（财联社、东方财富、巨潮资讯网）；或
- 本地维护的历史新闻语料库。

### ✅ Phase 4 第一轮迭代：生产化尝试（2026-07-05）

已完成 1、3、4 的初步实验，2 仍受数据源限制。

#### 4.1 改进 factor_agent（已完成）

新增 `factor_agent.py` v2：
- `--exclude-composites`：强制只用原始列；
- `--orthogonal`：剔除与现有特征平均截面 rank 相关度 > 0.85 的因子；
- `--min-icir` / `--min-positive-ic-ratio`：稳定性筛选；
- `--append-to-dataset`：自动生成带新因子的数据集。

运行结果：
```bash
python scripts/factor_agent.py \
  --prompt "Generate cross-sectional A-share alpha factors from raw price, fundamental, and flow features" \
  --dataset memory/dataset/features_h10_composite.parquet \
  --horizon 10 --exclude-composites --orthogonal --second-gen \
  --ic-threshold 0.03 --min-icir 0.5 --min-positive-ic-ratio 0.55 --max-avg-corr 0.85 \
  --append-to-dataset memory/dataset/features_h10_composite_llm_v2.parquet
```

筛选出 18 个稳定且相对独立的因子（全部基于原始列），top 3：

| 因子 | 表达式 | 样本内 IC | ICIR |
|---|---|---|---|
| llm_top1 | `(roe * net_mf_ratio / (volatility_20 + 1e-9)) * (relative_strength * dv_ratio)` | +0.1007 | +4.46 |
| llm_top2 | `(roe * net_mf_ratio / (volatility_20 + 1e-9)) + (relative_strength * dv_ratio)` | +0.0990 | +4.39 |
| llm_top3 | `((mom_20 + forecast_pchange_mid) / (volatility_20 + 1e-9)) * (relative_strength * dv_ratio)` | +0.0913 | +3.93 |

**但 walk-forward 效果（20 只等权，扣成本）**：

| 模型 | 平均超额 | 累计超额 | 胜率 | Top20 命中率 |
|---|---|---|---|---|
| composite 基线 | **+0.29%** | **+40.2%** | **53.3%** | 48.5% |
| + top 3 LLM 因子 | +0.14% | +16.2% | **55.2%** | 46.7% |
| + 18 个 LLM 因子 | +0.10% | +10.7% | 46.7% | 46.4% |

> 结论：LLM 生成的因子样本内 IC 很高，但进入 GBDT 后没有提升收益，反而稀释了原组合。top 3 虽然把胜率提到 55.2%，但平均超额和累计超额都明显下降。说明：
> - 这些因子与现有 composite 因子存在信息重叠；
> - 高样本内 IC 不等于 walk-forward 增量；
> - 需要更严格的**样本外特征选择**（真正用滚动验证集筛选），而不是全样本 IC 筛选。

#### 4.2 宏观择时（已完成）

新增 `scripts/apply_market_timing.py`：
- 用 `northbound_net_20d_zscore` 和 `margin_change_5d` 构建 `regime_score ∈ [-1, 1]`；
- `position_scale = clip(1 + regime_score, 0, 1)`；
- 当宏观信号负面时自动减仓/空仓。

**近似绩效（未扣成本）**：

| | 平均超额 | 累计超额 | 胜率 |
|---|---|---|---|
| composite 基线 | 0.68% | 89.1% | 60.4% |
| + 宏观择时 | 0.71% | 98.3% | 60.4% |

> 结论：宏观择时带来少量**毛收益提升**（累计 +9.2%），但胜率不变。若扣除交易成本，优势会缩小。可作为一种风险缓释手段，但不足以突破胜率瓶颈。

#### 4.3 事件过滤/增强（已完成）

新增 `scripts/apply_event_filter.py`：
- 基于 `disclosure_near` / `disclosure_very_near` / `since_disclosure_lt10` 做事件 overlay；
- 测试 `strict`（只选事件股）和 `prefer`（优先事件股）。

结果：
- `strict` 模式下，top20 候选中几乎没有事件标志股票，组合为空；
- `prefer` 模式退化为 baseline；
- 事件特征未被 GBDT 选为重要信号，单独作为选股条件无增量。

> 结论：`disclosure` 类特征在这个窗口内不稳定，不适合作为独立选股条件。

#### 4.4 真实文本/事件数据源（已打通 AKShare）

新增 `scripts/akshare_event_risk.py`，用 AKShare 抓取个股公告：
- 数据源：`ak.stock_individual_notice_report`（东方财富个股公告，支持历史日期区间）；
- 抓取范围：取每期 top-20 候选中出现频率最高的 80 只股票，覆盖 **61.2%** 的 top-20 持仓槽位；
- 风险打分：公告标题关键词规则（负面 - 正面）；
- **防穿越**：T 日决策只使用 **T-1 个交易日及之前** 发布的公告，避免把 T 日收盘后才披露的公告当作 T 日已知信息；
- 过滤：剔除 event_risk_score >= 3 的股票，用后续低分股票补齐。

**回测结果（gross，等权 top-20）**：

| | 平均超额 | 累计超额 | 胜率 |
|---|---|---|---|
| composite 基线 | 0.68% | 89.1% | 60.4% |
| + AKShare 公告风险过滤（threshold=3） | 0.67% | 88.4% | 60.4% |

> 结论：在严格防穿越后，公告风险过滤没有带来超额收益提升，只是略微降低波动。这说明：
> - GBDT 本身已经倾向于避开问题股；
> - 公告过滤更像“尾部保险”而非 alpha 来源；
> - 关键词规则过于粗糙，若接入 LLM 语义判断，可能还有优化空间。

同时更新了 `scripts/llm_event_filter.py`：
- 优先用 AKShare 拉公告；
- 无 OPENAI_API_KEY 时自动使用规则关键词打分；
- 保留 LLM 接口，配置 API 后可做更 nuanced 的语义判断。

实测：
```bash
python scripts/llm_event_filter.py --date 20250402 --ts-codes 000001.SZ,600519.SH,300125.SZ --lookback-days 10 --risk-threshold 2
```
- `300125.SZ`（*ST 美晨）被识别为高风险，分数 10.0；
- 平安银行、贵州茅台为中性 0.0。

#### 4.5 Walk-forward 阈值校准（已完成）

新增 `scripts/walkforward_threshold.py`：
- 每期用过去 N 期的预测数据，在候选分位数阈值 q ∈ [0.5, 0.9] 中选出训练窗口内表现最好的 q；
- T 日只选预测得分 >= T 日 q 分位数的股票；
- 完全基于历史数据校准阈值，避免在全样本上优化。

**回测结果（扣成本，等权 top-20，metric=win_rate）**：

| 训练窗口 | 平均超额 | 累计超额 | 胜率 |
|---|---|---|---|
| 无阈值 baseline | 0.29% | 40.2% | 53.3% |
| 6 期 | 0.30% | 41.2% | 54.3% |
| 12 期 | **0.36%** | **53.2%** | **55.2%** |
| 18 期 | 0.38% | 56.8% | 56.2% |
| 24 期 | 0.39% | 59.3% | 56.2% |

> 结论：walk-forward 阈值校准是**有效且未穿越**的改进。用过去 12 期（约 2.5 个月）数据选阈值，胜率从 53.3% 提升到 55.2%，累计超额从 40.2% 提升到 53.2%。训练窗口更长（18/24 期）效果略好，但提升边际递减，12 期作为默认参数已较稳健。

已接入生产链路：

1. `walkforward_gbdt.py` 支持 `--use-wf-threshold --wf-metric win_rate`；
2. `retrain_gbdt.py` 支持 `--wf-threshold --wf-metric win_rate`，训练后会在模型同目录生成 `_threshold.json`；
3. `gbdt_predictor.py` 加载模型时自动读取 `_threshold.json`，在 live 选股时对当日候选做同分位数过滤。

当前生产模型 `memory/models/gbdt_latest_h10.lightgbm.txt` 已附带阈值配置：
```json
{"q": 0.8, "metric": "win_rate", "max_positions": 20}
```

用法：
```bash
# 回测链路
python scripts/walkforward_gbdt.py \
  --dataset memory/dataset/features_h10_composite.parquet \
  --horizon 10 --use-wf-threshold --wf-metric win_rate

# 重训练并生成阈值配置
python scripts/retrain_gbdt.py \
  --dataset memory/dataset/features_h10_composite.parquet \
  --horizon 10 --wf-threshold --wf-metric win_rate

# live 选股（predictor 自动加载阈值配置）
python scripts/screen.py regime 20250402 30 --use-gbdt
```

#### 4.6 头寸加权 / 仓位优化（已完成）

新增 `portfolio_backtest.py` 的 `--weight-scheme` 参数，支持四种持仓权重方案：

| 方案 | 权重计算 | 说明 |
|---|---|---|
| `equal` | 等权 | 基线 |
| `score` | ∝ `max(0, predicted)` | 按预测置信度加权 |
| `risk_parity` | ∝ `1 / σ_20` | 按最近 20 日收益率波动率倒数加权 |
| `score_risk` | ∝ `predicted / σ_20` | 置信度与风险倒数结合 |

实现要点：
- 个股波动率完全基于缓存收盘价、决策日及之前的数据计算，无 look-ahead；
- 为加速风险平价计算，新增 `_close_by_code` 反向索引，把每次波动率计算从全缓存扫描降到 O(window)；
- `walkforward_gbdt.py` 同步支持 `--weight-scheme`。

**回测结果（扣成本，h=10，20 只，composite-only GBDT）**：

| 权重方案 | 平均超额 | 累计超额 | 胜率 | 年化夏普* |
|---|---|---|---|---|
| 等权 baseline | +0.29% | +40.15% | 53.3% | 0.50 |
| 按得分加权 | +0.37% | +44.77% | 49.5% | 0.42 |
| 风险平价 | +0.21% | +28.27% | 53.3% | 0.42 |
| 得分/波动率 | +0.23% | +25.05% | 48.6% | 0.30 |
| 等权 + WF 阈值 | +0.36% | +53.22% | **55.2%** | **0.61** |
| 得分加权 + WF 阈值 | +0.39% | +48.60% | 48.6% | 0.44 |

\* 年化夏普 = (平均超额 × 25.2) / (超额收益标准差 × √25.2)，按 10 日持有期年化。

> 结论：
> - **按预测得分加权**能提升平均/累计超额，但胜率下降、波动增大，风险调整后夏普更低；
> - **风险平价**直接压缩了高波动但高贡献股票的仓位，导致收益明显下降；
> - **得分/波动率**结合方案表现最差，说明直接用得分除以波动率会把噪声放大；
> - 与 walk-forward 阈值配合时，等权仍是风险收益比最优的选择。
>
> 因此 **生产链路默认仍使用等权 top-20**，`--weight-scheme score` 仅作为可选实验开关保留。后续若要做仓位优化，建议先尝试：
> 1. 行业/市值权重约束（把当前“行业数量上限”升级为“行业市值权重上限 + 风格中性”）；
> 2. 对 top 5 / top 10 集中仓位，而不是线性分散到 20 只；
> 3. 组合层面波动率缩放（宏观 regime 低迷时整体减仓），而非个股风险平价。

#### 4.7 当前仍未解决

1. **更大规模的 AKShare 回填**：80 只高频股票只能覆盖 61% 持仓，若要全覆盖 344 只 unique top-20 股票，抓取时间需 20~30 分钟，需改为离线批量任务或购买更稳定数据源；
2. **`memory_search`**：HelixAgent 服务端报错，需等官方修复或自建本地 RAG。

### 🚧 Phase 4 下一步（可选）

1. **更严格的 walk-forward 特征选择**：
   - 每期只用训练集 IC 筛选因子，在验证集上评估，最后才进入测试集；
   - 或先用 Lasso 在训练集上筛选，再交给 GBDT。
2. **把宏观择时接入真实回测**：在 `portfolio_backtest.py` 中实现 `position_multiplier`，扣成本后看净收益。
3. **接入外部新闻/公告 API**：让 `llm_event_filter.py` 真正产生文本信号。
4. **尝试多头/空头分离或行业中性**：看是否因为某些行业偏离导致胜率瓶颈。

### 🚧 Phase 5：监控与告警（1~2 周）

1. 参考 RD-Agent，让 LLM 阅读研报/论文生成因子代码；
2. 自动接入回测计算 IC；
3. IC 达标后纳入主模型；
4. 建立因子库与版本管理。

**验收标准**：自动生成至少 3 个 IC 显著的因子。

### Phase 5：生产化（2 周）

1. 修复 `memory_search`，启用 RAG；
2. 多 Agent 风控框架；
3. cron 自动调度；
4. 监控与告警（模型衰减、数据异常）。

---

## 11. 风险与应对

| 风险 | 应对 |
|---|---|
| GBDT 过拟合 | 严格 train/val/test 分离；用早停、正则化、purged CV |
| LLM 幻觉 | 不直接用 LLM 预测价格；只用于结构化信号和代码生成；输出必须有可验证来源 |
| Lookahead bias | 所有特征和标签只能用决策点之前数据；财报按 `ann_date` 过滤 |
| 数据质量 | 接入多个数据源交叉验证；异常值检测 |
| 高换手 | 加入换手率惩罚、交易成本；限制再平衡频率 |
| 市场环境变化 | 滚动训练 + regime 感知 + 模型漂移监控 |

---

## 12. 结论

AlphaHelix 的下一版应该是：

> **LightGBM/XGBoost 负责预测，LLM 负责信号增强与风控，walk-forward 负责验证，IC/IR 驱动进化。**

不要再用在线线性二分类追逐短期方向，也不要让 LLM 直接决定买卖。先把 GBDT 基线做到 rank IC 显著为正，再逐步叠加 LLM 因子和 Agent 风控。
